const form=document.getElementById('discipleForm'),error=document.getElementById('formError'),success=document.getElementById('success');form.addEventListener('submit',async e=>{e.preventDefault();error.textContent='';const fd=new FormData(form),body=Object.fromEntries(fd.entries());body.attested=fd.has('attested');body.weeklyPostCommitment=fd.has('weeklyPostCommitment');if(body.typedLegalName.trim().toLowerCase()!==body.name.trim().toLowerCase()){error.textContent='Your typed legal name must match the legal name above.';return;}const btn=form.querySelector('button[type=submit]');btn.disabled=true;try{const r=await fetch('/api/disciples/apply',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to submit application.');form.hidden=true;success.hidden=false;success.innerHTML='<span class="kicker">APPLICATION RECEIVED</span><h2>Welcome to the next step.</h2><p>Your reference is <strong>'+d.applicationReference+'</strong>.</p><p>Kingdom Vibe will review your application. If approved, you will receive your personal Disciple link and launch instructions by email.</p>';}catch(err){error.textContent=err.message;btn.disabled=false;}});